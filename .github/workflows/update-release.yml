# The update stage of the release pipeline: repo-owned, generated once by
# Vivswan/repo-platform and never overwritten by the sync. The managed
# ci.yml's `update-release` job calls this workflow between cutting the
# draft release (its `release` leg) and the attested publish (its
# `publish-release` leg), so everything this repository wants on the
# release happens here: packaging, asset uploads, note edits. Add jobs
# freely - publishing waits for every job in this file, and every asset
# uploaded here is attested at publish time into the release's single
# attestation.jsonl.
#
# The website deploys from ci.yml's `pages` job on every green push to
# main and from pages.yml nightly and on dispatch (root = newest vX.Y.Z
# tag, latest/ = main HEAD, one vX.Y.Z/ per served tag). Nothing runs on
# the tag itself, so a new release reaches the root through the release
# commit's own run, the nightly rebuild, or a manual dispatch.
# The REPO_PLATFORM_TOKEN secret (a PAT) is required for the full flow:
# without a PAT the release PR gets no CI runs (so it cannot pass the
# required all-green check without a manual close/reopen).

name: Update Release

on:
  workflow_call:
    inputs:
      tag:
        description: The draft release's tag (forced at creation, so it is checkout-able before publication)
        required: true
        type: string

jobs:
  update-release:
    name: build zips, upload, publish to store
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write # upload assets; also what makes the draft visible to gh
    steps:
      # Build from the TAG, not the branch head: the artifacts must be exactly
      # the released source even if main moved on since.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.tag }}

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build store zips (chrome + firefox)
        run: bun run build:chrome && bun run build:firefox

      - name: Verify store zips (version, name, no dev key)
        run: bun run verify:zips

      # Provenance is attested at publish time by ci.yml's publish-release leg
      # (public repositories only; non-public releases publish unattested):
      # one attestation.jsonl on the release covers every asset uploaded
      # here (chrome, firefox, AMO sources), verifiable with
      # `gh attestation verify <zip> -R <owner>/<repo> --bundle attestation.jsonl`.
      - name: Upload zips to the draft release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ inputs.tag }}
        run: |
          ls -la apps/extension/.output/*.zip
          gh release upload "$TAG" apps/extension/.output/*.zip --clobber

      # Publish the SAME chrome zip to both Chrome Web Store listing IDs via
      # `wxt submit`: the Polly listing, which became the unified "Cloud
      # Speech" listing (renamed in place), and the legacy Azure listing,
      # kept updated for backwards compatibility. One shared CWS OAuth app
      # (client id/secret/refresh token) + one extension-ID secret per
      # listing. Skips with a notice while the secrets are not configured,
      # so releases keep working before store automation is set up.
      # Docs: https://wxt.dev/guide/essentials/publishing
      - name: Publish to the Chrome Web Store (configured listing IDs)
        env:
          CHROME_CLIENT_ID: ${{ secrets.CWS_CLIENT_ID }}
          CHROME_CLIENT_SECRET: ${{ secrets.CWS_CLIENT_SECRET }}
          CHROME_REFRESH_TOKEN: ${{ secrets.CWS_REFRESH_TOKEN }}
          CWS_ID_POLLY: ${{ secrets.CWS_EXTENSION_ID_POLLY }}
          CWS_ID_AZURE: ${{ secrets.CWS_EXTENSION_ID_AZURE }}
        working-directory: apps/extension
        run: |
          if [ -z "$CHROME_CLIENT_ID" ] || [ -z "$CHROME_CLIENT_SECRET" ] || [ -z "$CHROME_REFRESH_TOKEN" ]; then
            echo "::notice::CWS_* secrets not fully configured; zips attached to the GitHub release only."
            exit 0
          fi
          version="$(node -p "require('../../package.json').version")"
          # Resolve by suffix glob: the filename pattern is written down only
          # in wxt.config.ts; verify:zips already asserted exactly one match.
          chrome_zip=$(ls .output/*-"${version}"-chrome.zip)
          for listing in polly azure; do
            id_var="CWS_ID_$(echo "$listing" | tr '[:lower:]' '[:upper:]')"
            extension_id="${!id_var}"
            if [ -z "$extension_id" ]; then
              echo "::notice::No extension id secret for $listing; skipping that listing."
              continue
            fi
            echo "Publishing $listing listing ($extension_id)..."
            CHROME_EXTENSION_ID="$extension_id" \
              bunx wxt submit --chrome-zip "$chrome_zip"
          done

      # Publish the firefox zip (+ AMO-required sources zip) to
      # addons.mozilla.org. Skips with a notice until the AMO_* secrets exist;
      # the FIRST submission must be done by hand on the AMO Developer Hub
      # (listing creation), after which the API keys make this automatic.
      - name: Publish to addons.mozilla.org
        env:
          FIREFOX_JWT_ISSUER: ${{ secrets.AMO_JWT_ISSUER }}
          FIREFOX_JWT_SECRET: ${{ secrets.AMO_JWT_SECRET }}
          FIREFOX_EXTENSION_ID: ${{ secrets.AMO_EXTENSION_ID }}
        working-directory: apps/extension
        run: |
          if [ -z "$FIREFOX_JWT_ISSUER" ] || [ -z "$FIREFOX_JWT_SECRET" ] || [ -z "$FIREFOX_EXTENSION_ID" ]; then
            echo "::notice::AMO_* secrets not configured; firefox zip attached to the GitHub release only."
            exit 0
          fi
          version="$(node -p "require('../../package.json').version")"
          bunx wxt submit \
            --firefox-zip "$(ls .output/*-"${version}"-firefox.zip)" \
            --firefox-sources-zip "$(ls .output/*-"${version}"-firefox-sources.zip)"
