{
  "name": "@cloud-speech/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Setup-guide website for Cloud Speech (vivswan.github.io/cloud-speech).",
  "scripts": {
    "dev": "astro dev stop; ASTRO_DEV_BACKGROUND=1 astro dev",
    "build": "astro build && bun scripts/generate-sitemap.mjs && bun scripts/check-links.mjs && bun scripts/check-theme-init.mjs",
    "preview": "astro preview",
    "typecheck": "astro check && tsc --noEmit"
  },
  "dependencies": {
    "@cloud-speech/constants": "workspace:*",
    "@cloud-speech/ui-tokens": "workspace:*"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/ibm-plex-sans": "^5.2.8",
    "@tailwindcss/vite": "^4.2.4",
    "astro": "^7.0.7",
    "tailwindcss": "~4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
