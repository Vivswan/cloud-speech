---
import Callout from "../../../components/Callout.astro";
import OnThisPage from "../../../components/OnThisPage.astro";
import Step from "../../../components/Step.astro";
import TroubleshootItem from "../../../components/TroubleshootItem.astro";
import Base from "../../../layouts/Base.astro";
import { shortcuts } from "../../../lib/site";
---

<Base
  title="OpenAI 设置 · Cloud Speech"
  description="为 Cloud Speech 浏览器扩展逐步设置 OpenAI: 创建 OpenAI 账号、生成 API 密钥并连接扩展。"
  active="openai"
>
  <main class="relative mx-auto max-w-3xl px-6 py-12">
    <p>
      <a class="link text-sm text-body" href="../../#guides">← 返回全部指南</a>
    </p>

    <header class="mt-8">
      <p
        class="inline-flex items-center gap-2 rounded-full bg-openai/10 px-3 py-1 text-xs font-medium text-body"
      >
        <span class="h-2 w-2 rounded-full bg-openai"></span>
        设置指南
      </p>
      <h1 class="mt-4 text-3xl font-bold tracking-tight sm:text-4xl">设置 OpenAI</h1>
      <p class="mt-4 leading-relaxed text-body">
        OpenAI 提供富有表现力的文字转语音语音 (tts-1、tts-1-hd 和
        gpt-4o-mini-tts)，是四家云服务商中设置最简单的: 一个 API
        密钥，无需选择区域，也不用在云控制台里绕圈子。大约 5 分钟即可完成。
      </p>
      <OnThisPage
        items={[
          { href: "#steps", label: "设置步骤" },
          { href: "#costs", label: "费用" },
          { href: "#remove", label: "移除访问权限" },
          { href: "#troubleshooting", label: "疑难解答" },
        ]}
      />
    </header>

    <div class="card mt-8 p-5">
      <h2 class="text-sm font-semibold">您需要准备</h2>
      <ul class="mt-2 list-disc space-y-1 pl-5 text-sm leading-relaxed text-body">
        <li>一个已设置结算的 OpenAI 平台账号 (语音服务没有免费额度)</li>
        <li>大约 5 分钟</li>
      </ul>
    </div>

    <ol
      id="steps"
      class="relative mt-12 scroll-mt-24 space-y-12 before:absolute before:top-2 before:bottom-2 before:left-4 before:w-px before:bg-fill"
    >
      <Step n={1} title="创建 OpenAI 账号">
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>
            打开<a class="link" href="https://platform.openai.com">platform.openai.com</a>并注册
            (或登录)
          </li>
          <li>
            打开 <strong>Settings → Billing</strong>，添加付款方式或购买预付额度; API
            按量计费，与 ChatGPT 订阅相互独立
          </li>
        </ol>
        <p class="text-muted">提示: 预付额度可以给您的总支出封顶。</p>
      </Step>

      <Step n={2} title="生成 API 密钥">
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>
            打开<a class="link" href="https://platform.openai.com/api-keys"
              >platform.openai.com/api-keys</a
            >
          </li>
          <li>点击 <strong>Create new secret key</strong></li>
          <li>命名，例如 <span class="value">Cloud Speech extension</span></li>
          <li>
            点击 <strong>Create secret key</strong> 并复制密钥 (以
            <span class="value">sk-</span> 开头)
          </li>
        </ol>
        <Callout title="安全警告">
          请立即复制密钥，OpenAI
          之后不会再显示它。请妥善保密; 拿到密钥的人都能花掉您的额度。您随时可以在同一页面撤销它。
        </Callout>
      </Step>

      <Step n={3} title="连接扩展">
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>点击浏览器工具栏中的扩展图标</li>
          <li>打开<strong>设置 → OpenAI</strong></li>
          <li>粘贴您的 <strong>API 密钥</strong></li>
          <li>点击<strong>保存并测试</strong></li>
        </ol>
        <p>
          测试通过后，OpenAI 的语音就会出现在语音选择器中。按 ▶ 可试听任意语音，点 ★
          收藏喜欢的语音。然后在任意页面上选中文字，点击右键选择<em>朗读</em>，或按
          <kbd>{shortcuts.readAloud}</kbd>。
        </p>
        <Callout title="小提示" tone="neutral">
          OpenAI 语音<strong>只支持语速调节</strong>。音调和音量滑块对它们无效
          (对 Polly、Azure 和 Google 语音有效)。
        </Callout>
      </Step>
    </ol>

    <!-- Costs -->
    <section class="card mt-12 scroll-mt-24 p-6" id="costs">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-openai"></span>
        费用
      </h2>
      <p class="mt-3 text-sm leading-relaxed text-body">
        OpenAI 的文字转语音<strong>没有免费额度</strong>。粗略估算，
        <strong>tts-1</strong> 约为<strong>每 100 万字符 $15</strong>，
        <strong>tts-1-hd</strong> 约为其两倍。<strong>gpt-4o-mini-tts</strong>
        计费方式不同，按文本输入和音频输出 token
        而非字符计费，但每分钟音频的实际花费与 tts-1
        大致相当。请查看<a class="link" href="https://platform.openai.com/docs/pricing"
          >OpenAI 官方价格页面</a
        >和我们的<a class="link" href="../../pricing/">服务商对比</a>。
      </p>
    </section>

    <!-- Remove access -->
    <section class="card mt-12 scroll-mt-24 p-6" id="remove">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-openai"></span>
        用完后移除访问权限
      </h2>
      <ol class="mt-3 list-decimal space-y-2 pl-5 text-sm leading-relaxed text-body">
        <li>
          打开<a class="link" href="https://platform.openai.com/api-keys">platform.openai.com/api-keys</a>，
          找到您创建的密钥，选择 <strong>Revoke</strong>。密钥会立即失效。
        </li>
        <li>
          在扩展中打开<strong>设置 → OpenAI</strong>，清除已保存的密钥 (或关闭该服务商)。
        </li>
      </ol>
      <p class="mt-3 text-sm leading-relaxed text-body">
        OpenAI 没有用于密钥管理的命令行工具，密钥只能在其控制台页面创建和撤销。
      </p>
    </section>

    <!-- Troubleshooting -->
    <section class="mt-12 scroll-mt-24" id="troubleshooting">
      <h2 class="text-xl font-semibold tracking-tight">疑难解答</h2>
      <div class="card mt-4 divide-y divide-edge-soft">
        <TroubleshootItem title='"凭据缺失或无效"'>
          <p>
            请仔细核对粘贴的密钥; 多出的空格是最常见的原因。确认密钥没有被撤销，且账号已设置结算或还有剩余额度。
            然后再次点击<strong>保存并测试</strong>。
          </p>
        </TroubleshootItem>
        <TroubleshootItem title='"拒绝访问"或速率限制错误'>
          <p>
            请确认账号状态正常、还有剩余额度，且没有超出速率限制。随着用量增长，限额会自动上调。
          </p>
        </TroubleshootItem>
      </div>
      <p class="mt-3 text-sm text-body">
        没有声音、语音丢失或键盘快捷键问题，请参阅<a class="link" href="../../troubleshooting/">疑难解答指南</a>。
      </p>
    </section>

    <p class="mt-10 text-sm leading-relaxed text-body">
      想用其他服务商? 参见<a class="link" href="../polly/">Amazon Polly</a>、<a class="link" href="../azure/">Azure Speech</a>或<a class="link" href="../google/">Google Cloud TTS</a>。
    </p>
  </main>
</Base>
