---
import Callout from "../../../components/Callout.astro";
import OnThisPage from "../../../components/OnThisPage.astro";
import Step from "../../../components/Step.astro";
import TroubleshootItem from "../../../components/TroubleshootItem.astro";
import Base from "../../../layouts/Base.astro";
import { shortcuts } from "../../../lib/site";
---

<Base
  title="Azure Speech 设置 · Cloud Speech"
  description="为 Cloud Speech 浏览器扩展逐步设置 Azure Speech: 创建免费 Azure 账号和语音资源，并连接密钥与区域。"
  active="azure"
>
  <main class="relative mx-auto max-w-3xl px-6 py-12">
    <p>
      <a class="link text-sm text-body" href="../../#guides">← 返回全部指南</a>
    </p>

    <header class="mt-8">
      <p
        class="inline-flex items-center gap-2 rounded-full bg-azure/10 px-3 py-1 text-xs font-medium text-body"
      >
        <span class="h-2 w-2 rounded-full bg-azure"></span>
        设置指南
      </p>
      <h1 class="mt-4 text-3xl font-bold tracking-tight sm:text-4xl">设置 Azure Speech</h1>
      <p class="mt-4 leading-relaxed text-body">
        微软的 Azure Speech
        服务提供覆盖大量语言的高品质神经语音，而且免费额度<em>每月重置</em>，很适合经常收听的用户。
        本指南带您一步步完成设置，大约需要 10 分钟。
      </p>
      <OnThisPage
        items={[
          { href: "#steps", label: "设置步骤" },
          { href: "#costs", label: "费用" },
          { href: "#remove", label: "移除访问权限" },
          { href: "#cli", label: "CLI 设置" },
          { href: "#troubleshooting", label: "疑难解答" },
        ]}
      />
    </header>

    <div class="card mt-8 p-5">
      <h2 class="text-sm font-semibold">您需要准备</h2>
      <ul class="mt-2 list-disc space-y-1 pl-5 text-sm leading-relaxed text-body">
        <li>
          一个 Microsoft 账号和一个免费的 Azure 账号 (注册时需要绑定付款方式)
        </li>
        <li>大约 10 分钟</li>
      </ul>
    </div>

    <ol
      id="steps"
      class="relative mt-12 scroll-mt-24 space-y-12 before:absolute before:top-2 before:bottom-2 before:left-4 before:w-px before:bg-fill"
    >
      <Step n={1} title="创建免费 Azure 账号">
        <p>如果您还没有 Azure 账号:</p>
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>
            打开<a class="link" href="https://azure.microsoft.com/free/">azure.microsoft.com/free</a>
          </li>
          <li>
            点击<strong>免费开始</strong>，用 Microsoft 账号登录 (或注册一个)
          </li>
          <li>完成身份验证并添加付款方式</li>
        </ol>
        <p class="text-muted">
          免费 (F0) 语音层不会产生任何费用。银行卡只用于身份验证，以及您明确选择的付费层级。
        </p>
      </Step>

      <Step n={2} title="创建语音资源">
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>
            登录<a class="link" href="https://portal.azure.com">Azure 门户</a>
          </li>
          <li>点击<strong>创建资源</strong>，搜索 <strong>Speech</strong></li>
          <li>
            选择<strong>Speech</strong> (由 Microsoft 提供，在"AI +
            机器学习"分类下)，点击<strong>创建</strong>
          </li>
          <li>选择订阅，并创建 (或选择) 一个<strong>资源组</strong></li>
          <li>
            选择离您较近的<strong>区域</strong>，例如 <span class="value">eastus</span>、
            <span class="value">westeurope</span> 或 <span class="value">southeastasia</span>
          </li>
          <li>给资源起一个名字，例如 <span class="value">cloud-speech-tts</span></li>
          <li>
            <strong>定价层</strong>选择<strong>免费 (F0)</strong>; 以后需要更多用量时，可以升级到标准 (S0)
          </li>
          <li>点击<strong>查看 + 创建</strong>，再点击<strong>创建</strong></li>
        </ol>
        <p class="text-muted">
          记住您选的区域，下一步扩展会用到它。
        </p>
      </Step>

      <Step n={3} title="复制密钥和区域">
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>部署完成后，点击<strong>转到资源</strong></li>
          <li>
            在左侧菜单中打开<strong>密钥和终结点</strong> (在"资源管理"下)
          </li>
          <li>复制<strong>密钥 1</strong> (两个密钥任选其一)</li>
          <li>
            记下同一页面上显示的<strong>位置/区域</strong>，例如
            <span class="value">eastus</span>
          </li>
        </ol>
        <Callout title="安全警告">
          请像对待密码一样对待这个密钥; 拿到它的人都能使用您的语音资源。您随时可以在同一页面重新生成密钥。
        </Callout>
      </Step>

      <Step n={4} title="连接扩展">
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>点击浏览器工具栏中的扩展图标</li>
          <li>打开<strong>设置 → Azure Speech</strong></li>
          <li>粘贴您的<strong>密钥</strong></li>
          <li>输入您的<strong>区域</strong> (例如 <span class="value">eastus</span>)</li>
          <li>点击<strong>保存并测试</strong></li>
        </ol>
        <p>
          测试通过后，Azure 的语音就会出现在语音选择器中。按 ▶ 可试听任意语音，点 ★
          收藏喜欢的语音。然后在任意页面上选中文字，点击右键选择<em>朗读</em>，或按
          <kbd>{shortcuts.readAloud}</kbd>。
        </p>
      </Step>
    </ol>

    <!-- Costs -->
    <section class="card mt-12 scroll-mt-24 p-6" id="costs">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-azure"></span>
        费用
      </h2>
      <p class="mt-3 text-sm leading-relaxed text-body">
        <strong>免费 (F0)</strong> 层每月包含约
        <strong>50 万神经语音字符</strong>，而且每月重置，不只限于第一年。标准 (S0) 层的神经语音约为
        <strong>每 100 万字符 $15-16</strong>; HD
        和自定义语音更贵。价格因区域而异，也会随时间调整，请以<a
          class="link"
          href="https://azure.microsoft.com/pricing/details/cognitive-services/speech-services/"
          >Azure Speech 官方价格页面</a
        >和我们的<a class="link" href="../../pricing/">服务商对比</a>为准。
      </p>
    </section>

    <!-- Remove access -->
    <section class="card mt-12 scroll-mt-24 p-6" id="remove">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-azure"></span>
        用完后移除访问权限
      </h2>
      <ol class="mt-3 list-decimal space-y-2 pl-5 text-sm leading-relaxed text-body">
        <li>
          在<a class="link" href="https://portal.azure.com">Azure 门户</a>中搜索
          您的语音资源 (或打开它所在的资源组)。
        </li>
        <li>
          选中该资源，选择<strong>删除</strong>。两个密钥都会立即失效。
          删除整个资源组会移除其中的所有内容。
        </li>
        <li>
          在扩展中打开<strong>设置 → Azure Speech</strong>，清除已保存的密钥
          (或关闭该服务商)。
        </li>
      </ol>
    </section>

    <!-- CLI alternative -->
    <section class="card mt-12 scroll-mt-24 p-6" id="cli">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-azure"></span>
        更喜欢用命令行?
      </h2>
      <p class="mt-3 text-sm leading-relaxed text-body">
        使用 <a class="link" href="https://learn.microsoft.com/cli/azure/install-azure-cli">Azure
        CLI</a>，以下命令可创建与上述步骤相同的免费层语音资源:
      </p>
      <pre class="cli-block mt-4"><code is:raw>az login
az group create --name tts --location eastus
az cognitiveservices account create --name my-speech --resource-group tts \
  --kind SpeechServices --sku F0 --location eastus --yes
az cognitiveservices account keys list --name my-speech --resource-group tts
# Paste key1 into the extension, with region "eastus".</code></pre>
      <p class="mt-4 text-sm leading-relaxed text-body">之后要全部移除:</p>
      <pre class="cli-block mt-4"><code is:raw>az cognitiveservices account delete --name my-speech --resource-group tts
az group delete --name tts --yes</code></pre>
    </section>

    <!-- Troubleshooting -->
    <section class="mt-12 scroll-mt-24" id="troubleshooting">
      <h2 class="text-xl font-semibold tracking-tight">疑难解答</h2>
      <div class="card mt-4 divide-y divide-edge-soft">
        <TroubleshootItem title='"凭据缺失或无效"'>
          <p>
            请仔细核对粘贴的值; 多出的空格或少复制了字符是最常见的原因。确认区域与您的语音资源一致 (例如
            <span class="value">eastus</span>)，且密钥没有被重新生成过。然后再次点击
            <strong>保存并测试</strong>。
          </p>
        </TroubleshootItem>
        <TroubleshootItem title='"拒绝访问"错误'>
          <p>
            您的密钥有效，但缺少权限或配额。请确认资源没有被移动或删除、账号状态正常，且没有超出
            F0 层的每月配额。
          </p>
        </TroubleshootItem>
      </div>
      <p class="mt-3 text-sm text-body">
        没有声音、语音丢失或键盘快捷键问题，请参阅<a class="link" href="../../troubleshooting/">疑难解答指南</a>。
      </p>
    </section>

    <p class="mt-10 text-sm leading-relaxed text-body">
      想用其他服务商? 参见<a class="link" href="../polly/">Amazon Polly</a>、<a class="link" href="../google/">Google Cloud TTS</a>或<a class="link" href="../openai/">OpenAI</a>。
    </p>
  </main>
</Base>
