---
import Callout from "../../../components/Callout.astro";
import OnThisPage from "../../../components/OnThisPage.astro";
import Step from "../../../components/Step.astro";
import TroubleshootItem from "../../../components/TroubleshootItem.astro";
import Base from "../../../layouts/Base.astro";
import { shortcuts } from "../../../lib/site";
---

<Base
  title="Amazon Polly 设置 · Cloud Speech"
  description="为 Cloud Speech 浏览器扩展逐步设置 Amazon Polly: 创建 AWS 账号、IAM 用户和访问密钥，并连接扩展。"
  active="polly"
>
  <main class="relative mx-auto max-w-3xl px-6 py-12">
    <p>
      <a class="link text-sm text-body" href="../../#guides">← 返回全部指南</a>
    </p>

    <header class="mt-8">
      <p
        class="inline-flex items-center gap-2 rounded-full bg-polly/10 px-3 py-1 text-xs font-medium text-body"
      >
        <span class="h-2 w-2 rounded-full bg-polly"></span>
        设置指南
      </p>
      <h1 class="mt-4 text-3xl font-bold tracking-tight sm:text-4xl">设置 Amazon Polly</h1>
      <p class="mt-4 leading-relaxed text-body">
        Amazon Polly 是 AWS 的文字转语音服务，提供 Standard、Neural、Generative 和 Long-form
        引擎的数百种语音。本指南从零开始，带您一步步创建 AWS 凭据。无需开发经验，大约 10
        分钟即可完成。
      </p>
      <OnThisPage
        items={[
          { href: "#steps", label: "设置步骤" },
          { href: "#costs", label: "费用" },
          { href: "#remove", label: "移除访问权限" },
          { href: "#cli", label: "CLI 设置" },
          { href: "#troubleshooting", label: "疑难解答" },
        ]}
      />
    </header>

    <div class="card mt-8 p-5">
      <h2 class="text-sm font-semibold">您需要准备</h2>
      <ul class="mt-2 list-disc space-y-1 pl-5 text-sm leading-relaxed text-body">
        <li>一个 AWS 账号 (提供免费额度; 注册时需要绑定付款方式)</li>
        <li>大约 10 分钟</li>
      </ul>
    </div>

    <ol
      id="steps"
      class="relative mt-12 scroll-mt-24 space-y-12 before:absolute before:top-2 before:bottom-2 before:left-4 before:w-px before:bg-fill"
    >
      <Step n={1} title="创建 AWS 账号">
        <p>如果您还没有 AWS 账号:</p>
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>打开<a class="link" href="https://aws.amazon.com">aws.amazon.com</a></li>
          <li>点击<strong>创建 AWS 账户</strong></li>
          <li>按提示完成注册流程</li>
          <li>
            添加付款方式 (免费额度可以覆盖基础用量; 见下方费用说明)
          </li>
        </ol>
        <p class="text-muted">已经有 AWS 账号? 直接跳到第 2 步。</p>
      </Step>

      <Step n={2} title="创建拥有 Polly 权限的 IAM 用户">
        <p>
          出于安全考虑，请创建一个<em>只能</em>使用 Polly
          的专用用户。切勿在扩展中使用根账号凭据:
        </p>
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>
            登录<a class="link" href="https://console.aws.amazon.com">AWS 控制台</a>
          </li>
          <li>搜索并打开 <strong>IAM</strong> 服务</li>
          <li>点击<strong>用户 → 创建用户</strong></li>
          <li>输入用户名，例如 <span class="value">cloud-speech-extension</span></li>
          <li>选择<strong>直接附加策略</strong></li>
          <li>搜索并选择 <span class="value">AmazonPollyReadOnlyAccess</span></li>
          <li>点击<strong>创建用户</strong></li>
        </ol>
      </Step>

      <Step n={3} title="生成访问密钥">
        <p>创建扩展与 Polly 通信所需的密钥:</p>
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>点击您刚创建的用户</li>
          <li>打开<strong>安全凭证</strong>标签页</li>
          <li>点击<strong>创建访问密钥</strong></li>
          <li>选择<strong>在 AWS 外部运行的应用程序</strong></li>
          <li>
            添加描述，例如 <span class="value">Cloud Speech extension</span>
          </li>
          <li>点击<strong>创建访问密钥</strong></li>
          <li>
            复制<strong>访问密钥 ID</strong>和<strong>私有访问密钥</strong>，<strong>两者都要</strong>
          </li>
        </ol>
        <Callout title="安全警告">
          请立即保存您的私有访问密钥，AWS 之后不会再显示它。妥善保管这些凭据，切勿泄露给他人。
        </Callout>
      </Step>

      <Step n={4} title="选择区域">
        <p>选择离您最近的 AWS 区域以获得更好的性能。常见选择:</p>
        <ul class="list-disc space-y-1.5 pl-5 marker:text-faint">
          <li><span class="value">us-east-1</span>，弗吉尼亚北部 (最便宜)</li>
          <li><span class="value">us-west-2</span>，俄勒冈</li>
          <li><span class="value">eu-west-1</span>，爱尔兰</li>
          <li><span class="value">ap-southeast-1</span>，新加坡</li>
        </ul>
        <p>
          完整列表见<a class="link" href="https://docs.aws.amazon.com/general/latest/gr/pol.html"
            >AWS Polly 区域列表</a
          >。注意并非每个引擎 (例如 Generative) 在所有区域都可用。
        </p>
      </Step>

      <Step n={5} title="连接扩展">
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>点击浏览器工具栏中的扩展图标</li>
          <li>打开<strong>设置 → Amazon Polly</strong></li>
          <li>粘贴您的<strong>访问密钥 ID</strong></li>
          <li>粘贴您的<strong>私有访问密钥</strong></li>
          <li>
            输入您选择的<strong>区域</strong> (例如 <span class="value">us-east-1</span>)
          </li>
          <li>点击<strong>保存并测试</strong></li>
        </ol>
        <p>
          测试通过后，Polly 的语音就会出现在语音选择器中。按 ▶ 可试听任意语音，点 ★
          收藏喜欢的语音。然后在任意页面上选中文字，点击右键选择<em>朗读</em>，或按
          <kbd>{shortcuts.readAloud}</kbd>。
        </p>
      </Step>
    </ol>

    <!-- Costs -->
    <section class="card mt-12 scroll-mt-24 p-6" id="costs">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-polly"></span>
        费用
      </h2>
      <p class="mt-3 text-sm leading-relaxed text-body">
        AWS 的免费额度在您注册后的前 12 个月内，每月包含<strong>500
        万标准语音字符</strong>和<strong>100 万神经语音字符</strong>。之后 (或超出免费额度后):
      </p>
      <ul
        class="mt-3 list-disc space-y-1.5 pl-5 text-sm leading-relaxed text-body marker:text-faint"
      >
        <li><strong>标准语音:</strong> 每 100 万字符 $4.00</li>
        <li><strong>神经语音:</strong> 每 100 万字符 $16.00</li>
        <li><strong>生成式语音:</strong> 每 100 万字符 $30.00</li>
        <li><strong>长文本语音:</strong> 每 100 万字符 $100.00</li>
      </ul>
      <p class="mt-3 text-sm leading-relaxed text-body">
        每个字符都计费，包括空格。参见<a class="link" href="https://aws.amazon.com/polly/pricing/"
          >Polly 官方价格页面</a
        >和我们的<a class="link" href="../../pricing/">服务商对比</a>。
      </p>
    </section>

    <!-- Remove access -->
    <section class="card mt-12 scroll-mt-24 p-6" id="remove">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-polly"></span>
        用完后移除访问权限
      </h2>
      <ol class="mt-3 list-decimal space-y-2 pl-5 text-sm leading-relaxed text-body">
        <li>
          打开<a class="link" href="https://console.aws.amazon.com/iam/home#/users">IAM
          用户</a>页面，选择您创建的用户 (例如
          <span class="value">polly-tts</span>)。
        </li>
        <li>
          在<strong>安全凭证</strong>标签页找到该访问密钥，选择
          <strong>操作 → 停用</strong>，再选择<strong>删除</strong>。密钥会立即失效。
        </li>
        <li>要彻底清理，可用<strong>删除用户</strong>删掉该用户本身。</li>
        <li>
          在扩展中打开<strong>设置 → Amazon Polly</strong>，清除已保存的密钥
          (或关闭该服务商)。
        </li>
      </ol>
    </section>

    <!-- CLI alternative -->
    <section class="card mt-12 scroll-mt-24 p-6" id="cli">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-polly"></span>
        更喜欢用命令行?
      </h2>
      <p class="mt-3 text-sm leading-relaxed text-body">
        使用以管理员身份登录的 <a class="link" href="https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html">AWS
        CLI</a>，以下命令可创建与上述步骤相同的最小权限用户和密钥:
      </p>
      <pre class="cli-block mt-4"><code is:raw>aws iam create-user --user-name polly-tts

aws iam put-user-policy --user-name polly-tts --policy-name polly-synthesize \
  --policy-document '{"Version":"2012-10-17","Statement":[{"Effect":"Allow",
    "Action":["polly:SynthesizeSpeech","polly:DescribeVoices"],"Resource":"*"}]}'

aws iam create-access-key --user-name polly-tts
# Paste AccessKeyId and SecretAccessKey into the extension; pick any region.</code></pre>
      <p class="mt-4 text-sm leading-relaxed text-body">之后要全部移除:</p>
      <pre class="cli-block mt-4"><code is:raw>aws iam list-access-keys --user-name polly-tts
aws iam delete-access-key --user-name polly-tts --access-key-id AKIA...
aws iam delete-user-policy --user-name polly-tts --policy-name polly-synthesize
aws iam delete-user --user-name polly-tts</code></pre>
    </section>

    <!-- Troubleshooting -->
    <section class="mt-12 scroll-mt-24" id="troubleshooting">
      <h2 class="text-xl font-semibold tracking-tight">疑难解答</h2>
      <div class="card mt-4 divide-y divide-edge-soft">
        <TroubleshootItem title='"凭据缺失或无效"'>
          <p>
            请仔细核对粘贴的值; 多出的空格或少复制了字符是最常见的原因。确认 IAM
            用户已附加
            <span class="value">AmazonPollyReadOnlyAccess</span> 策略、区域填写正确
            (例如 <span class="value">us-east-1</span>)，且访问密钥在 AWS
            控制台中仍处于激活状态。然后再次点击<strong>保存并测试</strong>。
          </p>
        </TroubleshootItem>
        <TroubleshootItem title='"拒绝访问"错误'>
          <p>
            您的凭据有效，但缺少权限。请附加
            <span class="value">AmazonPollyReadOnlyAccess</span>，并确认所选引擎
            (例如 Generative) 在您选择的区域可用。
          </p>
        </TroubleshootItem>
      </div>
      <p class="mt-3 text-sm text-body">
        没有声音、语音丢失或键盘快捷键问题，请参阅<a class="link" href="../../troubleshooting/">疑难解答指南</a>。
      </p>
    </section>

    <p class="mt-10 text-sm leading-relaxed text-body">
      想用其他服务商? 参见<a class="link" href="../azure/">Azure Speech</a>、<a class="link" href="../google/">Google Cloud TTS</a>或<a class="link" href="../openai/">OpenAI</a>。
    </p>
  </main>
</Base>
