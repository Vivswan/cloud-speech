---
import Callout from "../../../components/Callout.astro";
import OnThisPage from "../../../components/OnThisPage.astro";
import Step from "../../../components/Step.astro";
import TroubleshootItem from "../../../components/TroubleshootItem.astro";
import Base from "../../../layouts/Base.astro";
import { shortcuts } from "../../../lib/site";
---

<Base
  title="OpenAI 兼容设置 · Cloud Speech"
  description="将 Groq、DeepInfra 等托管的 OpenAI 兼容文字转语音服务或 LiteLLM 代理连接到 Cloud Speech 浏览器扩展。"
  active="custom"
>
  <main class="relative mx-auto max-w-3xl px-6 py-12">
    <p>
      <a class="link text-sm text-body" href="../../#guides">← 返回全部指南</a>
    </p>

    <header class="mt-8">
      <p
        class="inline-flex items-center gap-2 rounded-full bg-custom/10 px-3 py-1 text-xs font-medium text-body"
      >
        <span class="h-2 w-2 rounded-full bg-custom"></span>
        设置指南
      </p>
      <h1 class="mt-4 text-3xl font-bold tracking-tight sm:text-4xl">OpenAI 兼容</h1>
      <p class="mt-4 leading-relaxed text-body">
        不少服务通过与 OpenAI 的
        <span class="value">/v1/audio/speech</span> 端点相同的 API
        提供语音模型，扩展可以直接与它们通信。想在自己的电脑上运行模型？参见<a class="link" href="../local/">使用任何其他模型</a>。托管服务则无需安装任何东西：注册账号、生成
        API 密钥，再把服务的基础 URL
        填给扩展即可。费用按使用量支付给该服务。大约 5 分钟即可完成。
      </p>
      <OnThisPage
        items={[
          { href: "#steps", label: "设置步骤" },
          { href: "#services", label: "哪些服务可用" },
          { href: "#costs", label: "费用" },
          { href: "#remove", label: "移除访问权限" },
          { href: "#troubleshooting", label: "疑难解答" },
        ]}
      />
    </header>

    <div class="card mt-8 p-5">
      <h2 class="text-sm font-semibold">您需要准备</h2>
      <ul class="mt-2 list-disc space-y-1 pl-5 text-sm leading-relaxed text-body">
        <li>在一家提供 OpenAI 兼容文字转语音的服务上注册的账号</li>
        <li>大约 5 分钟</li>
      </ul>
    </div>

    <ol
      id="steps"
      class="relative mt-12 scroll-mt-24 space-y-12 before:absolute before:top-2 before:bottom-2 before:left-4 before:w-px before:bg-fill"
    >
      <Step n={1} title="选择服务">
        <p>已知按这种 API 形式提供文字转语音的服务：</p>
        <ul class="list-disc space-y-1.5 pl-5 marker:text-faint">
          <li><a class="link" href="https://groq.com">Groq</a></li>
          <li><a class="link" href="https://deepinfra.com">DeepInfra</a></li>
          <li>
            <a class="link" href="https://together.ai">Together AI</a>：一个端点即可使用多个模型家族，包括
            Cartesia Sonic、MiniMax Speech、Rime、Orpheus 和 Kokoro
          </li>
        </ul>
      </Step>

      <Step n={2} title="创建账号和 API 密钥">
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>在服务的网站上注册；若有要求，先设置结算</li>
          <li>
            在其控制台生成一个 <strong>API 密钥</strong>并复制；如果服务支持命名，可叫
            <span class="value">Cloud Speech extension</span> 之类
          </li>
          <li>
            在其 API 文档中找到该服务的 <strong>OpenAI 兼容基础 URL</strong>，通常以
            <span class="value">/v1</span> 结尾
          </li>
        </ol>
        <Callout title="安全警告">
          请妥善保管密钥；拿到它的人都能消耗您的账户余额。您随时可以在同一控制台撤销它。
        </Callout>
      </Step>

      <Step n={3} title="连接扩展">
        <ol class="list-decimal space-y-1.5 pl-5 marker:text-faint">
          <li>点击浏览器工具栏中的扩展图标</li>
          <li>打开<strong>设置 → OpenAI 兼容</strong></li>
          <li>把服务的基础 URL 粘贴为<strong>服务器 URL</strong></li>
          <li>粘贴您的 <strong>API 密钥</strong>（托管服务都需要）</li>
          <li>
            可选：填写用逗号分隔的<strong>语音名称</strong>，以及服务文档中给出的
            <strong>模型</strong>（默认为
            <span class="value">tts-1</span>）
          </li>
          <li>点击<strong>保存并测试</strong></li>
        </ol>
        <p>
          扩展会先向服务请求它的语音列表。如果服务不报告语音，就回退到 OpenAI
          的九个标准语音名称，而您在语音栏中输入的内容优先级最高。测试通过后，在任意页面上选中文字，
          点击右键选择<em>朗读</em>，或按 <kbd>{shortcuts.readAloud}</kbd>。
        </p>
      </Step>
    </ol>

    <!-- Service compatibility -->
    <section class="card mt-12 scroll-mt-24 p-6" id="services">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-custom"></span>
        各项服务的支持情况
      </h2>
      <div class="mt-3 space-y-3 text-sm leading-relaxed text-body">
        <p>
          <strong>可直接使用。</strong>LocalAI、Speaches 和 openedai-speech（见<a class="link" href="../local/">使用任何其他模型</a>），以及
          Groq、DeepInfra、Together AI 等托管网关。Together AI 可直接使用 Cartesia 和 MiniMax
          语音：它的服务器 URL 是 <span class="value">https://api.together.ai/v1</span>，
          其<a class="link" href="https://docs.together.ai/docs/text-to-speech">文档</a>列出了可粘贴到扩展语音和模型栏的语音名称与模型
          id（例如
          <span class="value">hexgrad/Kokoro-82M</span>）。
        </p>
        <p>
          <strong>需要自己运行一个代理。</strong>ElevenLabs 和 Vertex AI/Gemini 使用各自的 API
          格式；LiteLLM 代理可以把它们转换成这种端点形式，如果您更愿意走代理而不是 Together
          AI，MiniMax 也可以这样接入。LiteLLM 的运行方法见<a class="link" href="../local/">使用任何其他模型</a>。
        </p>
      </div>
    </section>

    <!-- Costs -->
    <section class="card mt-12 scroll-mt-24 p-6" id="costs">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-custom"></span>
        费用
      </h2>
      <p class="mt-3 text-sm leading-relaxed text-body">
        各项服务自行定价；开始前请先查看其价格页面：<a class="link" href="https://groq.com/pricing">Groq 价格</a>、<a class="link" href="https://deepinfra.com/pricing">DeepInfra 价格</a>，或您所选服务的价格页面。要了解托管服务与云服务商的差异，请参阅我们的<a class="link" href="../../pricing/">服务商对比</a>。
      </p>
    </section>

    <!-- Remove access -->
    <section class="card mt-12 scroll-mt-24 p-6" id="remove">
      <h2 class="flex items-center gap-2.5 text-lg font-semibold">
        <span class="h-2.5 w-2.5 rounded-full bg-custom"></span>
        用完后移除访问权限
      </h2>
      <ol class="mt-3 list-decimal space-y-2 pl-5 text-sm leading-relaxed text-body">
        <li>
          在服务的控制台中撤销 API 密钥。密钥会立即失效。
        </li>
        <li>
          在扩展中打开<strong>设置 → OpenAI 兼容</strong>，清除已保存的字段
          （或关闭该服务商）。
        </li>
      </ol>
    </section>

    <!-- Troubleshooting -->
    <section class="mt-12 scroll-mt-24" id="troubleshooting">
      <h2 class="text-xl font-semibold tracking-tight">疑难解答</h2>
      <div class="card mt-4 divide-y divide-edge-soft">
        <TroubleshootItem title="401 或未授权错误">
          <p>
            粘贴的密钥有误或已被撤销。请在服务的控制台生成新密钥，粘贴到扩展的 API
            密钥栏，再次点击
            <strong>保存并测试</strong>。密钥前后多出的空格也是常见原因。
          </p>
        </TroubleshootItem>
        <TroubleshootItem title="404 或找不到资源错误">
          <p>
            基础 URL 有误：缺少 <span class="value">/v1</span> 后缀，或指向了错误的路径。
            请严格按照服务 API 文档给出的 OpenAI 兼容基础 URL 复制，然后重新保存。
          </p>
        </TroubleshootItem>
        <TroubleshootItem title="语音选择器中没有语音，或语音不对">
          <p>
            扩展会先向服务请求语音列表，失败时回退到 OpenAI
            的标准名称。如果服务的语音没有显示出来，请在其文档中查到名称，用逗号分隔填入
            <strong>语音名称</strong>栏，然后重新保存。
          </p>
        </TroubleshootItem>
      </div>
      <p class="mt-3 text-sm text-body">
        没有声音、语音丢失或键盘快捷键问题，请参阅<a class="link" href="../../troubleshooting/">疑难解答指南</a>。
      </p>
    </section>

    <p class="mt-10 text-sm leading-relaxed text-body">
      想用我们没有列出的模型，或者想要免费的自托管方案？请改用<a class="link" href="../local/">使用任何其他模型</a>。也可以选择一家云服务商：<a class="link" href="../polly/">Amazon Polly</a>、<a class="link" href="../azure/">Azure Speech</a>、<a class="link" href="../google/">Google Cloud TTS</a>或<a class="link" href="../openai/">OpenAI</a>。
    </p>
  </main>
</Base>
